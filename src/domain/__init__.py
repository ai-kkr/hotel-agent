"""Domain layer — pure business logic, entities, value objects, events, and ports.

This layer depends on nothing external (no infrastructure, no presentation). It defines the
behaviour and the ports (interfaces) that outer layers implement.
"""
