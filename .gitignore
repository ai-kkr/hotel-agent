# Python
__pycache__/
*.py[cod]
*$py.class
.Python
.venv/
venv/
env/

# Testing / coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
*.cover

# Environment
.env

# Tooling caches
.ruff_cache/
.ty_cache/

# FlightAPI on-disk cache (cashews DiskCache backend) — runtime artifact, never committed.
.cache/

# Booking-corpus collector (dev tooling) — personal PII must never enter the repo.
credentials.json
token.json
*.eml
corpus_output/
.kkr-hotel-corpus/

# OS
.DS_Store
Thumbs.db
privatekey.pem

# Railway IaC — TypeScript runner SDK (railway-iac-ts). Локально ставится через
# `npm install` в .railway/; бинарник резолвится через RAILWAY_IAC_TS_BIN.
.railway/node_modules/
