## 1. Каркас пакета и зависимости

- [x] 1.1 Создать каталог `scripts/` на корне репо (сиблинг `src/`) с `__init__.py` и точкой входа CLI (например `scripts/collect_corpus.py`).
- [x] 1.2 Добавить `ezgmail` в `[project.optional-dependencies].dev` через `uv` (`uv add --optional dev ezgmail`).
- [x] 1.3 Пополнить `.gitignore`: папка вывода корпуса, `credentials.json`, `token.json`/refresh-токен `ezgmail`.
- [x] 1.4 Добавить `scripts/README.md` с инструкцией: где взять OAuth Desktop credential, первый запуск consent, папка вывода, предупреждение про PII.

## 2. Доступ к Gmail (D1)

- [x] 2.1 Реализовать модуль `scripts/gmail_source.py`: инициализация `ezgmail` (`init`/credentials), обёртка `search(query)` поверх `ezgmail.search`, возвращающая нормализованных кандидатов (subject/from/body/date + raw для `.eml`).
- [x] 2.2 Обрабатывать ошибки аутентификации/протухшего токена с понятным сообщением и инструкцией повторить consent (тест: мок `ezgmail` бросает ошибку → ожидаемое сообщение).
- [x] 2.3 Параметризовать поисковый запрос (env/CLI), по умолчанию — широкий запрос по подтверждениям брони от разных систем.

## 3. Классификация и извлечение (D2)

- [x] 3.1 Описать Pydantic-схему результата классификации (`is_hotel_confirmation`, `system/sender`, `confidence`, причина пропуска).
- [x] 3.2 Реализовать батч-классификатор через `model.with_structured_output(Schema)` по списку кандидатов; порог уверенности — параметр.
- [x] 3.3 Тест классификатора на fake-модели (без живого LLM): смесь реальных/нерелевантных кандидатов → корректный отбор и логирование пропусков.

## 4. Отбор N разнообразных (D3)

- [x] 4.1 Реализовать `select_diverse(classified, n)`: first-seen-per-система, fallback-добор при недостатке уникальных систем.
- [x] 4.2 Логировать фактическое число найденных кандидатов, число уникальных систем и число отобранных.
- [x] 4.3 Тест `select_diverse`: больше систем чем N / меньше систем чем N / пустой пул.

## 5. Переупаковка в форвард (D4)

- [x] 5.1 Реализовать `wrap_as_forward(original, *, client_email, recipient, wish_cover)`: сборка `email.message.Message` с `From`/`To`/`Subject: Fwd:` + обложка + `-----Original Message-----` + оригинал.
- [x] 5.2 Пул шаблонов обложек-пожеланий (или пустая обложка); выбор параметризуется.
- [x] 5.3 Сверить формат разделителя/обложки с тем, как `booking-intake` разделяет блоки (прочитать спеку/парсер; прогнать первое собранное письмо через intake вручную и убедиться, что разбор корректен).
- [x] 5.4 Тест `wrap_as_forward`: с обложкой и без → корректные заголовки и тело; сериализация в валидный `.eml`.

## 6. Сохранение результатов (D5/D6)

- [x] 6.1 Реализовать запись `N` `.eml` файлов в папку вывода (по умолчанию вне репо) + `manifest.json` (отель, система, дата, есть ли обложка).
- [x] 6.2 Тест записи: файлы созданы, `manifest.json` валиден, пути покрыты `.gitignore`.

## 7. CLI и сквозной прогон

- [x] 7.1 Параметры CLI: `--count` (по умолч. 10), поисковый запрос, `client-email`, `recipient` (`c.<token>@<mail-domain>`), папка вывода, порог уверенности, наличие/пул обложек.
- [x] 7.2 Сквозной тест с замоканным Gmail + fake-LLM: от поиска до N `.eml` + `manifest.json` без сети.
- [x] 7.3 (ручной, владельцем) Реальный прогон против живого Gmail; убедиться, что хотя бы одно собранное письмо корректно разбирается intake-цепочкой.

## 8. Качество

- [x] 8.1 `uv run ruff check` чист (для `scripts/` и `tests/scripts/`; один предсуществующий B017 в `tests/workflows/` к этому change не относится).
- [x] 8.2 `uv run ty check` чист.
- [x] 8.3 `uv run pytest` зелёный (199 passed, 3 skipped — gated Temporal E2E; coverage 90.43%).
